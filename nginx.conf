worker_processes auto;
events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    upstream router_ethereum {
        server wallet-api.0xpepper.com; # Internal URL for Ethereum router
    }

    upstream wallet_ethereum {
        server wallet-api.0xpepper.com; # Internal URL for Ethereum wallet
    }

    upstream router_base {
        server wallet-api.0xpepper.com; # Internal URL for Base chain router
    }

    upstream wallet_base {
        server wallet-api.0xpepper.com; # Internal URL for Base chain wallet
    }

    server {
        listen 80;
        server_name api.0xpepper.com;

        # Chain ID 1 routes (Ethereum)
        location /1/router {
            proxy_pass http://router_ethereum;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /1/wallet {
            proxy_pass http://wallet_ethereum;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Chain ID 8453 routes (Base)
        location /8453/router {
            proxy_pass http://router_base;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /8453/wallet {
            proxy_pass http://wallet_base;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
